 server {
    listen 80;
    <% if @os['family'] == 'RedHat' || @os['family'] == 'Debian'  -%>
    root /var/www;  #redhat or debian os family
    <% end -%>
    index index.html index.htm;

    server_name _;
}
